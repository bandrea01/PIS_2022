package View.ViewModel;

import DAO.Articolo.ArticoloDAO;
import DAO.Articolo.ImmagineDAO;
import DAO.Categoria.CategoriaDAO;
import DAO.Fornitore.FornitoreDAO;
import DAO.Produttore.ProduttoreDAO;
import Model.Categoria;
import Model.Fornitore;
import Model.Produttore;

import java.io.IOException;

public class DBMClass {
    public static void main(String[] args) throws IOException {
       /* Produttore produttore1 = new Produttore(1001,"Gazprom","www.gazprom.com","San Pietroburgo", "Russia");
        Fornitore fornitore = new Fornitore(1001,"Amazon", "www.amazon.com", "Seattle", "USA");
        Categoria categoria = new Categoria(1001, "Cucina", null);
        ImmagineDAO.getInstance().add(1000, "frigorifero.jpeg");
        ImmagineDAO.getInstance().removeSingleImage("frigorifero.jpeg");
        ArticoloDAO.getInstance().removeById(1000);
        ArticoloDAO.getInstance().removeById(1001);
        ArticoloDAO.getInstance().removeById(1002);
        ArticoloDAO.getInstance().removeById(1003);
        ArticoloDAO.getInstance().removeById(2000);
        ProduttoreDAO.getInstance().remove(produttore1);
        FornitoreDAO.getInstance().remove(fornitore);
        CategoriaDAO.getInstance().remove(categoria);*/
    }
}
